# Day 013
```python
# Side Quest: try, except, finally

# Debugging is important skill
if __name__ == "__main__":
    try:
        age = int(input("How old are you? "))

        if age > 18:
            print(f"You can drve at age {age}")
    except ValueError:
        print(ValueError)
    except Exception as e:
        print((e))

# Last Resort: ChatGPT or ask other developers
```
