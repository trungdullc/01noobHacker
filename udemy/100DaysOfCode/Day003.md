# Day 003
```python
# Note: Python has no switch/case statement (C/C++, Java, or JavaScript)
# Control Flow w/           if/else/elif
# Comparison operators      >, >=, <, <=, ==, !=
# Logical operators         and, or, not

age: int = 20

# teens get discount
if age <18:
    print("You can't drink")
elif age >= 18 and age < 21:                # elif 18 <= age < 21:
    print("You can drink in USA")
else:
    print("You to old to drink")

# Note: if forget method right click on str > Go to Definition or dir(str)
is_extra: str = input("Do you want supersize it for $2 more. [Y]es or [N]o : ")[0].upper()

if is_extra == "Y":
    print("That will be $2 extra")
else:
    print("Thank you")

# from python file convert to flowchart
# Source: https://pypi.org/project/pyflowchart/
PS C:\Users\hackerdu> python -m venv venv
PS C:\Users\hackerdu> .\venv\Scripts\activate
PS C:\Users\hackerdu> (venv) pip install pyflowchart
PS C:\Users\hackerdu> (venv) pyflowchart app.py
PS C:\Users\hackerdu> (venv) deactivate

# Manual: To much work
draw.io:        https://app.diagrams.net/
Lucid Charts:   https://www.lucidchart.com/pages
```

# Side Quest: Modulo
```python
def is_even(number: int) -> bool:
    """
    brute force
    """
    return number%2 == 0                                # return number & 1 == 0 (if == 1 means odd number, 8 4 2 1)

if __name__ == "__main__":
    print(is_even(2))
```

# Side Quest: ASCII Art
```
Optional Site: https://ascii.co.uk/art
```